


void drawGrid() {
  
     print(config.maxHeight);
    for (int x = 0; x < config.maxWidth; x++) {
        for (int y = 0; y < config.maxHeight; y++) {
              // code to exe
        }
    }
}
